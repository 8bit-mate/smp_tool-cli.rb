#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "./../lib/smp_tool/cli"

# ARGV = "z -i bin/smp0.bin -c 5 -a -o new.bin".split(" ")

begin
  Dry::CLI.new(SMPTool::CLI::Commands).call
  exit 0
rescue StandardError => e
  warn e
  exit 1
end
