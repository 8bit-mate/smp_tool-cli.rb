#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "./../lib/smp_tool/cli"

# ARGV = "new -o file.bas --basic=1 --dir_segs=2".split(" ")

begin
  Dry::CLI.new(SMPTool::CLI::Commands).call
  exit 0
rescue StandardError => e
  puts e
  exit 1
end
